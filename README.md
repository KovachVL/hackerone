# hackerone

# Start

.env.example is located in pt_crm/backend/app. You can use it for testing my project. The site itself runs on port 3000, and S3 on port 9001

```bash

cd pt_crm

docker compose up --build -d 
```